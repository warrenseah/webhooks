{
  "name": "webhookServer",
  "version": "1.0.0",
  "description": "webhook skeleton",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "production": "NODE_ENV=production pm2 start index.js --name webhookFlagone --cron '0 2 * * *' --time"
  },
  "keywords": [],
  "author": "TokenClan Warren Seah",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1"
  }
}
